# Dependencias directas del proyecto (retegi.eus). El resto las instala pip como subdependencias.
# Nota: django-allauth-ui instala slippers, django-widget-tweaks y PyYAML como dependencias.

# --- Core ---
Django==5.1.2

# Config y despliegue
python-decouple==3.8
dj-database-url==2.3.0
gunicorn==23.0.0
whitenoise==6.8.2
psycopg2-binary==2.9.10

# --- Apps Django usadas en código / settings ---
django-allauth==65.3.1
django-allauth-ui==1.5.3
django-celery-beat==2.7.0
django-parler==2.3
django-rosetta==0.10.0
django-tinymce==4.1.0

# Tareas y tiempo real (Celery, Channels, ASGI)
celery==5.4.0
channels==4.2.0
channels-redis==4.2.1
redis==5.2.1
daphne==4.1.2

# Integraciones HTTP / correo / asistente
httpx==0.28.1
sendgrid==6.11.0
openai==0.28.0

# Imágenes (ImageField)
pillow==10.4.0

# Script de traducciones (scripts/fill_home_i18n.py)
polib==1.2.0
PyJWT
